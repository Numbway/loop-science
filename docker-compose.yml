services:
  postgres:
    image: postgres:16-alpine
    container_name: loop-science-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: rc_user
      POSTGRES_PASSWORD: rc_pass
      POSTGRES_DB: research_companion
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rc_user -d research_companion"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: loop-science-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: rc-backend
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./storage:/data/projects
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: rc-worker
    restart: unless-stopped
    command: python -m celery -A app.core.celery_app.celery_app worker --loglevel=info --pool=solo
    volumes:
      - ./backend:/app
      - ./storage:/data/projects
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: rc-frontend
    restart: unless-stopped
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    env_file:
      - .env
    depends_on:
      - backend

  executor:
    build:
      context: ./executor
      dockerfile: Dockerfile
    image: loop-science-executor:latest
    profiles:
      - executor-build

volumes:
  postgres_data:
